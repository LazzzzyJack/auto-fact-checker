{
  "name": "auto-fact-checker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve": "nodemon server",
    "full": "concurrently -n 'server,client' -c 'magenta,blue' \"npm run serve\" \"npm run dev\""
  },
  "devDependencies": {
    "vite": "^4.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "openai": "^3.1.0"
  }
}
